@page
@model GenderHealthcareServiceManagementSystemPages.Pages.ServiceManagement.DeleteModel
@{
    ViewData["Title"] = "Xóa dịch vụ";
}

<main class="delete-service-page">
    <div class="container">
        <!-- Header -->
        <div class="page-header">
            <div class="breadcrumb">
                <a asp-page="./Index">Quản lý dịch vụ</a>
                <span>/</span>
                <span>Xóa dịch vụ</span>
            </div>
            <div class="warning-header">
                <div class="warning-icon">
                    <i class="bi bi-exclamation-triangle"></i>
                </div>
                <div class="warning-content">
                    <h1>Xác nhận xóa dịch vụ</h1>
                    <p>Hành động này không thể hoàn tác. Vui lòng xem xét kỹ trước khi thực hiện.</p>
                </div>
            </div>
        </div>

        <!-- Service Preview Card -->
        <div class="service-preview-card">
            <div class="card-header_delete">
                <div class="service-icon">
                    <i class="bi bi-heart-pulse"></i>
                </div>
                <div class="service-info">
                    <h2>@Model.Service.Name</h2>
                    <div class="status-badge @(Model.Service.IsDeleted ? "inactive" : "active")">
                        @if (Model.Service.IsDeleted)
                        {
                            <i class="bi bi-x-circle"></i>
                            <span>Tạm dừng</span>
                        }
                        else
                        {
                            <i class="bi bi-check-circle"></i>
                            <span>Hoạt động</span>
                        }
                    </div>
                </div>
                <div class="delete-indicator">
                    <i class="bi bi-trash"></i>
                </div>
            </div>

            <div class="card-content">
                <div class="details-grid">
                    <div class="detail-row">
                        <div class="detail-item">
                            <div class="detail-label">
                                <i class="bi bi-file-text"></i>
                                Mô tả
                            </div>
                            <div class="detail-value">
                                @if (!string.IsNullOrEmpty(Model.Service.Description))
                                {
                                    @Model.Service.Description
                                }
                                else
                                {
                                    <span class="no-data">Chưa có mô tả</span>
                                }
                            </div>
                        </div>
                    </div>

                    <div class="detail-row">
                        <div class="detail-item">
                            <div class="detail-label">
                                <i class="bi bi-currency-dollar"></i>
                                Giá dịch vụ
                            </div>
                            <div class="detail-value price">
                                @Model.Service.Price.ToString("N0") VND
                            </div>
                        </div>

                        <div class="detail-item">
                            <div class="detail-label">
                                <i class="bi bi-calendar-plus"></i>
                                Ngày tạo
                            </div>
                            <div class="detail-value">
                                @Model.Service.CreatedAt?.ToString("dd/MM/yyyy")
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Warning Message -->
        <div class="warning-message">
            <div class="warning-content">
                <div class="warning-icon-large">
                    <i class="bi bi-shield-exclamation"></i>
                </div>
                <div class="warning-text">
                    <h3>Cảnh báo quan trọng</h3>
                    <ul>
                        <li>Dịch vụ sẽ bị xóa vĩnh viễn khỏi hệ thống</li>
                        <li>Tất cả dữ liệu liên quan sẽ không thể khôi phục</li>
                        <li>Các lịch hẹn đã đặt có thể bị ảnh hưởng</li>
                        <li>Hành động này không thể hoàn tác</li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- Action Buttons -->
        <div class="action-section">
            <div class="action-buttons">
                <form method="post" class="delete-form">
                    <input type="hidden" asp-for="Service.ServiceId" />
                    <button type="button" class="btn btn-danger" id="confirmDeleteBtn">
                        <i class="bi bi-trash"></i>
                        Xác nhận xóa
                    </button>
                </form>

                <a asp-page="./Index" class="btn btn-secondary">
                    <i class="bi bi-arrow-left"></i>
                    Hủy và quay lại
                </a>

                <a asp-page="./Details" asp-route-id="@Model.Service.ServiceId" class="btn btn-info">
                    <i class="bi bi-eye"></i>
                    Xem chi tiết
                </a>
            </div>
        </div>

        <!-- Confirmation Modal -->
        <div class="modal-overlay" id="confirmModal">
            <div class="modal-content">
                <div class="modal-header">
                    <div class="modal-icon">
                        <i class="bi bi-exclamation-triangle"></i>
                    </div>
                    <h3>Xác nhận cuối cùng</h3>
                </div>
                <div class="modal-body">
                    <p>Bạn có chắc chắn muốn xóa dịch vụ <strong>"@Model.Service.Name"</strong> không?</p>
                    <div class="confirmation-input">
                        <label>Nhập "XÓA" để xác nhận:</label>
                        <input type="text" id="confirmationInput" placeholder="Nhập XÓA">
                    </div>
                </div>
                <div class="modal-actions">
                    <button type="button" class="btn btn-danger" id="finalDeleteBtn" disabled>
                        <i class="bi bi-trash"></i>
                        Xóa vĩnh viễn
                    </button>
                    <button type="button" class="btn btn-secondary" id="cancelBtn">
                        <i class="bi bi-x"></i>
                        Hủy
                    </button>
                </div>
            </div>
        </div>
    </div>
</main>

<style>
    /* Main Layout */
    .delete-service-page {
        min-height: 100vh;
        background: linear-gradient(135deg, #fef2f2 0%, #fee2e2 50%, #fecaca 100%);
        padding: 2rem 0;
    }

    .container {
        max-width: 900px;
        margin: 0 auto;
        padding: 0 1rem;
    }

    /* Header */
    .page-header {
        margin-bottom: 2rem;
    }

    .breadcrumb {
        font-size: 0.875rem;
        color: #7f1d1d;
        margin-bottom: 1rem;
    }

        .breadcrumb a {
            color: #dc2626;
            text-decoration: none;
        }

            .breadcrumb a:hover {
                text-decoration: underline;
            }

    .warning-header {
        display: flex;
        align-items: center;
        gap: 1rem;
        background: rgba(255, 255, 255, 0.9);
        backdrop-filter: blur(10px);
        padding: 1.5rem;
        border-radius: 12px;
        border-left: 4px solid #dc2626;
        box-shadow: 0 4px 15px rgba(220, 38, 38, 0.1);
    }

    .warning-icon {
        width: 3rem;
        height: 3rem;
        background: linear-gradient(135deg, #dc2626, #b91c1c);
        color: white;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        flex-shrink: 0;
    }

    .warning-content h1 {
        font-size: 1.5rem;
        font-weight: 700;
        color: #7f1d1d;
        margin-bottom: 0.5rem;
    }

    .warning-content p {
        color: #991b1b;
        margin: 0;
    }

    /* Service Preview Card */
    .service-preview-card {
        background: white;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        margin-bottom: 2rem;
        overflow: hidden;
        border: 2px solid #fecaca;
    }

    .card-header_delete {
        background: linear-gradient(135deg, #f3f4f6, #e5e7eb);
        padding: 1.5rem;
        display: flex;
        align-items: center;
        gap: 1rem;
        position: relative;
    }

    .service-icon {
        width: 3rem;
        height: 3rem;
        background: linear-gradient(135deg, #7c3aed, #a855f7);
        color: white;
        border-radius: 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
    }

    .service-info {
        flex: 1;
    }

        .service-info h2 {
            font-size: 1.25rem;
            font-weight: 700;
            color: #1f2937;
            margin-bottom: 0.5rem;
        }

    .status-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.25rem 0.75rem;
        border-radius: 50px;
        font-size: 0.75rem;
        font-weight: 500;
    }

        .status-badge.active {
            background: #d1fae5;
            color: #065f46;
        }

        .status-badge.inactive {
            background: #fee2e2;
            color: #991b1b;
        }

    .delete-indicator {
        width: 2.5rem;
        height: 2.5rem;
        background: linear-gradient(135deg, #dc2626, #b91c1c);
        color: white;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.25rem;
        animation: pulse 2s infinite;
    }

    @@keyframes pulse {
        0%, 100%

    {
        opacity: 1;
    }

    50% {
        opacity: 0.7;
    }

    }

    .card-content {
        padding: 1.5rem;
    }

    .details-grid {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .detail-row {
        display: grid;
        grid-template-columns: 1fr;
        gap: 1rem;
    }

        .detail-row:last-child {
            grid-template-columns: 1fr 1fr;
        }

    .detail-item {
        background: #f9fafb;
        padding: 1rem;
        border-radius: 8px;
        border-left: 3px solid #dc2626;
    }

    .detail-label {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-weight: 600;
        color: #374151;
        font-size: 0.875rem;
        margin-bottom: 0.5rem;
    }

        .detail-label i {
            color: #dc2626;
        }

    .detail-value {
        color: #1f2937;
        font-size: 0.875rem;
    }

        .detail-value.price {
            font-size: 1.125rem;
            font-weight: 700;
            color: #059669;
        }

    .no-data {
        color: #6b7280;
        font-style: italic;
    }

    /* Warning Message */
    .warning-message {
        background: linear-gradient(135deg, #fef2f2, #fee2e2);
        border: 2px solid #fecaca;
        border-radius: 12px;
        padding: 1.5rem;
        margin-bottom: 2rem;
    }

    .warning-content {
        display: flex;
        gap: 1rem;
    }

    .warning-icon-large {
        width: 3rem;
        height: 3rem;
        background: linear-gradient(135deg, #dc2626, #b91c1c);
        color: white;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        flex-shrink: 0;
    }

    .warning-text h3 {
        color: #7f1d1d;
        font-size: 1.125rem;
        font-weight: 700;
        margin-bottom: 0.75rem;
    }

    .warning-text ul {
        color: #991b1b;
        margin: 0;
        padding-left: 1.25rem;
    }

    .warning-text li {
        margin-bottom: 0.5rem;
    }

    /* Action Section */
    .action-section {
        background: white;
        border-radius: 12px;
        padding: 1.5rem;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    }

    .action-buttons {
        display: flex;
        gap: 1rem;
        justify-content: center;
    }

    .btn {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.75rem 1.5rem;
        border-radius: 8px;
        font-size: 0.875rem;
        font-weight: 500;
        text-decoration: none;
        border: none;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    .btn-danger {
        background: linear-gradient(135deg, #dc2626, #b91c1c);
        color: white;
    }

        .btn-danger:hover:not(:disabled) {
            background: linear-gradient(135deg, #b91c1c, #991b1b);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(220, 38, 38, 0.3);
        }

        .btn-danger:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

    .btn-secondary {
        background: #f3f4f6;
        color: #374151;
        border: 1px solid #d1d5db;
    }

        .btn-secondary:hover {
            background: #e5e7eb;
            color: #1f2937;
        }

    .btn-info {
        background: linear-gradient(135deg, #3b82f6, #1d4ed8);
        color: white;
    }

        .btn-info:hover {
            background: linear-gradient(135deg, #2563eb, #1e40af);
        }

    /* Modal */
    .modal-overlay {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0, 0, 0, 0.5);
        backdrop-filter: blur(4px);
        display: none;
        align-items: center;
        justify-content: center;
        z-index: 1000;
    }

        .modal-overlay.show {
            display: flex;
        }

    .modal-content {
        background: white;
        border-radius: 12px;
        padding: 2rem;
        max-width: 500px;
        width: 90%;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
        animation: modalSlideIn 0.3s ease;
    }

    @@keyframes modalSlideIn {
        from

    {
        opacity: 0;
        transform: translateY(-20px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }

    }

    .modal-header {
        text-align: center;
        margin-bottom: 1.5rem;
    }

    .modal-icon {
        width: 4rem;
        height: 4rem;
        background: linear-gradient(135deg, #dc2626, #b91c1c);
        color: white;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2rem;
        margin: 0 auto 1rem;
    }

    .modal-header h3 {
        color: #1f2937;
        font-size: 1.25rem;
        font-weight: 700;
        margin: 0;
    }

    .modal-body {
        margin-bottom: 1.5rem;
    }

        .modal-body p {
            color: #374151;
            text-align: center;
            margin-bottom: 1rem;
        }

    .confirmation-input {
        text-align: center;
    }

        .confirmation-input label {
            display: block;
            color: #374151;
            font-weight: 500;
            margin-bottom: 0.5rem;
        }

        .confirmation-input input {
            width: 200px;
            padding: 0.75rem;
            border: 2px solid #d1d5db;
            border-radius: 8px;
            text-align: center;
            font-weight: 600;
            text-transform: uppercase;
        }

            .confirmation-input input:focus {
                outline: none;
                border-color: #dc2626;
            }

    .modal-actions {
        display: flex;
        gap: 1rem;
        justify-content: center;
    }

    /* Responsive */
    @@media (max-width: 768px) {
        .container

    {
        padding: 0 0.5rem;
    }

    .warning-header {
        flex-direction: column;
        text-align: center;
    }

        .card-header_delete {
        flex-direction: column;
        text-align: center;
        gap: 1rem;
    }

    .detail-row:last-child {
        grid-template-columns: 1fr;
    }

    .warning-content {
        flex-direction: column;
        text-align: center;
    }

    .action-buttons {
        flex-direction: column;
    }

    .modal-actions {
        flex-direction: column;
    }

    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const confirmDeleteBtn = document.getElementById('confirmDeleteBtn');
        const confirmModal = document.getElementById('confirmModal');
        const cancelBtn = document.getElementById('cancelBtn');
        const confirmationInput = document.getElementById('confirmationInput');
        const finalDeleteBtn = document.getElementById('finalDeleteBtn');
        const deleteForm = document.querySelector('.delete-form');

        // Show modal
        confirmDeleteBtn.addEventListener('click', function() {
            confirmModal.classList.add('show');
            confirmationInput.focus();
        });

        // Hide modal
        cancelBtn.addEventListener('click', function() {
            confirmModal.classList.remove('show');
            confirmationInput.value = '';
            finalDeleteBtn.disabled = true;
        });

        // Close modal on overlay click
        confirmModal.addEventListener('click', function(e) {
            if (e.target === confirmModal) {
                confirmModal.classList.remove('show');
                confirmationInput.value = '';
                finalDeleteBtn.disabled = true;
            }
        });

        // Enable/disable final delete button
        confirmationInput.addEventListener('input', function() {
            const isValid = this.value.toUpperCase() === 'XÓA';
            finalDeleteBtn.disabled = !isValid;

            if (isValid) {
                finalDeleteBtn.style.background = 'linear-gradient(135deg, #dc2626, #b91c1c)';
            } else {
                finalDeleteBtn.style.background = '';
            }
        });

        // Final delete action
        finalDeleteBtn.addEventListener('click', function() {
            if (confirmationInput.value.toUpperCase() === 'XÓA') {
                // Add loading state
                this.innerHTML = '<i class="bi bi-hourglass-split"></i> Đang xóa...';
                this.disabled = true;

                // Submit form
                deleteForm.submit();
            }
        });

        // Keyboard shortcuts
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape' && confirmModal.classList.contains('show')) {
                confirmModal.classList.remove('show');
                confirmationInput.value = '';
                finalDeleteBtn.disabled = true;
            }

            if (e.key === 'Enter' && confirmModal.classList.contains('show') && !finalDeleteBtn.disabled) {
                finalDeleteBtn.click();
            }
        });
    });
</script>